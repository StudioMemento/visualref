{
  "name": "memento-visualref-v47r-core-recovery",
  "version": "47.0.2",
  "private": true,
  "type": "module",
  "description": "VisualRef V47R — persistent native V45 core, recovered World → Shot → Delta → Curve workflow.",
  "scripts": {
    "check": "node tests/check-syntax.mjs",
    "test:delta": "node tests/delta-engine.mjs",
    "test:static": "python3 tests/validate_package.py",
    "test:browser": "python3 tests/browser_recovery.py",
    "validate": "npm run check && npm run test:delta && npm run test:static && npm run test:browser"
  }
}
