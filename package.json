{
  "name": "memento-visualref-v46-polish",
  "version": "46.0.0",
  "private": true,
  "type": "module",
  "description": "MEMENTO VisualRef V46 — viewport-first guided world building, render control polish and usable timeline creation.",
  "scripts": {
    "check": "node --check src/v46/bootstrap.js && node --check src/v46/local-bootstrap.js && node --check src/v46/polish-controller.js && node --check scripts/install-v46.mjs"
  }
}
